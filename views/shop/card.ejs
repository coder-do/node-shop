<%- include('../includes/head.ejs') %> 
<%- include('../includes/nav.ejs') %>
    <main class="cart">
        <div style="margin-top: 80px;"></div>
        <% if (products.length > 0) { %>
            <ul class="cart__item-list">
                <% products.forEach(p => { %>
                    <li class="cart__item">
                        <div style="display: flex; justify-content: space-between;">
                            <p><%= p.id.title %> (x<%= p.quantity %>)</p>
                            <form action="/delete-card-item" method="POST">
                                <input type="hidden" value="<%= p.id._id %>" name="id">
                                <input type="hidden" name="_csrf" value="<%= token %>">
                                <button class="btn" type="submit" style="margin: 9px 0 0 6px;">Delete</button>
                            </form>
                        </div>
                    </li>
                <% }) %>
            </ul>
            <div class="centered">
                <form action="/order-page" method="POST">
                    <input type="hidden" name="_csrf" value="<%= token %>">
                    <button type="submit" class="btn">Order now</button>
                </form>
            </div>
        <% } else { %>
            <h1 style="text-align: center;">No Products in Card!</h1>
        <% } %>
    </main>
<%- include('../includes/end.ejs') %> 